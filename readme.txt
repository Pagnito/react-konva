npm install 
npm run dev
Project is served on localhost:3000 though you can specify w/e port in webpack.config.js
Go to webpack.config.js and specify the backend server to proxy your requests to. Default is '/api', '/auth' proxies to port 2000

